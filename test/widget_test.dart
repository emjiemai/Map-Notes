// Placeholder — the default flutter_create smoke test doesn't apply here
// (MapNotesApp needs Supabase initialized to render). Add real widget tests
// once there's a mockable repository seam worth testing against.
