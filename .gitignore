app.env

# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
*.g.dart

# Platform folders not in active use — regenerate locally with
# `flutter create --platforms=<name> .`. /android IS committed (CI builds
# need it checked out, not regenerated) — see README.
/ios
/macos
/windows
/linux
/web

# IDE
.idea/
*.iml
.vscode/

# Misc
*.log
